% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post_hoc.R
\name{post_hoc}
\alias{post_hoc}
\title{Compute pairwise post-hoc comparisons}
\usage{
post_hoc(x, method = "auto", alpha = 0.05, force = FALSE, conf_level = 0.95)
}
\arguments{
\item{x}{A \code{comp_spec} that has been fit with \code{\link[=test]{test()}}. If not yet fit,
\code{post_hoc()} will call \code{\link[=test]{test()}} automatically.}

\item{method}{Post-hoc method (see \code{\link[=set_post_hoc]{set_post_hoc()}}). Use "auto" to pick a
default based on the engine and data.}

\item{alpha}{Significance level used for gating (default 0.05).}

\item{force}{Logical; compute even if omnibus test is not significant.}

\item{conf_level}{Confidence interval level (default 0.95).}
}
\value{
If \code{x} is a spec, the updated spec with \verb{$post_hoc}; otherwise a
tibble of pairwise contrasts.
}
\description{
Computes pairwise contrasts between groups following a significant
omnibus test. The chosen method depends on the analysis engine, design and
outcome type, and can be overridden via \code{\link[=set_post_hoc]{set_post_hoc()}}.
}
\details{
The result is a tibble with one row per group contrast. If the omnibus
test is not significant (p ≥ \code{alpha}) and \code{force = FALSE}, a zero-row tibble
is returned with attributes \code{skipped = TRUE} and a \code{reason} message.
}
